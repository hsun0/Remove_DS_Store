[package]
name = "rmds"
version = "0.1.3"
edition = "2024"
rust-version = "1.97"
description = "Safely remove unwanted macOS metadata from ZIP archives, folders, and Git repositories"
license = "MIT"
keywords = ["cli", "zip", "macos", "metadata"]
categories = ["command-line-utilities", "filesystem"]

[dependencies]
tempfile = "3.27.0"
zip = { version = "8.6.0", default-features = false, features = ["deflate-flate2-zlib-rs"] }

[lints.rust]
unsafe_code = "forbid"
